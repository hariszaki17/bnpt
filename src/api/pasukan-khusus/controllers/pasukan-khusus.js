'use strict';

/**
 * pasukan-khusus controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::pasukan-khusus.pasukan-khusus');
